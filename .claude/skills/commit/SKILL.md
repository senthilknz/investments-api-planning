# Commit & Push Skill
1. Run `mvn test` if Java files were modified
2. Stage all changes with `git add -A`
3. Generate a clear, descriptive commit message summarizing changes
4. Commit and push to the current branch
5. Show the commit hash and summary
EOF